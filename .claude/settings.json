{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f topology.json ] && [ -n \"$(git status --porcelain)\" ]; then node dist/cli.js analyze && bash scripts/serve-review.sh; fi",
            "timeout": 130
          }
        ]
      }
    ]
  }
}
